/**
 * Activity feedback components.
 *
 * Each activity type has a corresponding feedback component that displays
 * submission results in a way appropriate for that type of problem.
 */

export { default as EiplFeedback } from './EiplFeedback.vue'
