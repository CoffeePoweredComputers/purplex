/**
 * Activity input components.
 *
 * Each activity type has a corresponding input component that handles
 * user interaction for that specific type of problem.
 */

export { default as EiplInput } from './EiplInput.vue'
